# Copy to .env.local for local dev, or set in the Vercel project's
# Environment Variables. This key is read ONLY server-side, in the
# serverless function. It is never exposed to the browser or bundled
# into any client-side code.
ANTHROPIC_API_KEY=sk-ant-...
